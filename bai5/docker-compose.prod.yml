version: '3.8'

services:
  backend:
    environment:
      - ENV=production
    restart: always
    deploy:
      mode: replicated
      replicas: 3
      resources:
        limits:
          cpus: '0.5'     
          memory: 512M   